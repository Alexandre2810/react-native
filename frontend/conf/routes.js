// Gestion des routes dans ce fichier

const mainpoint = "/messenger";

const routes = {
    home: `${mainpoint}/home`,
    about: `${mainpoint}/about`,
    register: `${mainpoint}/register`,
    login: `${mainpoint}/login`
}

export default routes;